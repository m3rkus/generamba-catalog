{% include 'header' %}

import UIKit

class {{module_info.name}}Initializer: NSObject {
    @IBOutlet weak var viewController: {{module_info.name}}Controller!

    override func awakeFromNib() {
        let configurator = {{module_info.name}}Configurator()
        configurator.configureViewInput(viewInput: viewController)
    }
}
